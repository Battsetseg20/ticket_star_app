# frozen_string_literal: true

class TicketsController < ApplicationController
  # before_action :authenticate_user!
  # if admin account is created, then actions related to tickets should be created here
end
